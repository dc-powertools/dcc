# Task Catalog

This is the canonical discovery and lifecycle record for every accepted task. Physical
row order has no scheduling meaning. The Root Orchestrator is the sole writer.

- Format: 1
- Next task ID: T-0080
- Primary task: None
- Scheduling: Running
- Global pause source or reason: None

## Tasks

| ID | Outcome | Authority / Rev | Status | Depends On | Route / Risk | Approval Or Blocker | Next Safe Action | Details | Result |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| T-0001 | Load agent files and initialize the development framework state. | User request 2026-07-14 / r1 | Done | None | Discover / Low | None | Stop; outcome complete. | None | Created the project cursor, task catalog, project brief, implementation context, source map, glossary, and standards. Verified `cargo fmt --check`, `cargo check`, `cargo clippy -- -D warnings`, `cargo test`, `cargo build`, and `cargo run -- --help`. |
| T-0002 | Evaluate `AGENTS.bak.md` and unexpected `readme/` files, merge durable guidance into framework state, and remove leftovers. | User request 2026-07-14 / r1 | Done | None | Quick change / Low | None | Stop; outcome complete. | None | Migrated the backup's project-doc load rules, `.gitignore` boundary, and Codex question-handling guidance into project context, standards, and source map. Removed `AGENTS.bak.md`. Confirmed there are no untracked files. |
| T-0003 | Migrate `readme/DEVELOPMENT.md`, `readme/STYLE.md`, and `readme/ARCHITECTURE.md` to strict framework ownership. | User request 2026-07-14 / r1 | Done | None | Quick change / Low | None | Stop; outcome complete. | None | Moved the legacy docs to `.meta/project/development.md`, `.meta/project/rust-style.md`, and `.meta/project/architecture.md`; updated cursor, context, standards, source map, glossary, and internal references so no unmanaged legacy doc paths remain. |
| T-0004 | Coordinate the full `dcc` rewrite for devcontainer schema-compatible config, state persistence, durable container lifecycle commands, and shell-oriented attach behavior. | User request 2026-07-14 + attach amendment + full-framework execution request / r3 | Done | Child tasks T-0005 through T-0010 | Initiative / High | None | Stop; outcome complete. | `.meta/tasks/0004-dcc-rewrite-brief.md`; `.meta/tasks/0004-dcc-rewrite-notes.md`; `.meta/quality/0004-dcc-rewrite-quality.md`; `.meta/threat-models/0004-dcc-runtime.md` | Completed schema-compatible `customizations.dcc`, state persistence, Feature metadata compatibility, build preparation, durable runtime lifecycle commands, final compatibility fields, unsafe runtime gating, docs, review, and follow-up official devcontainer CLI config validation. Live Docker smoke testing remains a recorded residual risk. |
| T-0005 | Implement devcontainer schema-compatible config parsing and merge behavior under `customizations.dcc`, including legacy `extends`/`scripts` warnings. | Parent T-0004 / r1 | Done | None | Initiative / High | None | Stop; outcome complete. | `.meta/tasks/0005-config-schema-brief.md` | Added `customizations.dcc.extends`, `commands`, and parser-level `state` support; normalized legacy top-level `extends`/`scripts` with deprecation warnings; preserved existing command resolution semantics; verified fmt, clippy, tests, and build; completed read-only specialist review with no blocking findings. |
| T-0006 | Implement validated `customizations.dcc.state` path model and profile-local state cache mount planning. | Parent T-0004 / r1 | Done | T-0005 | Initiative / High | None | Stop; outcome complete. | `.meta/tasks/0006-state-cache-brief.md` | Added state path substitution/validation, duplicate/conflict and overlap rejection, deferred runtime `${containerEnv:VAR}` resolution, profile-local state mount planning, host preparation for directory/file state, runtime/debug mount integration, docs, and tests. Verified fmt, clippy, tests, and build. |
| T-0007 | Update Feature metadata handling for state, commands, unsupported properties, unsafe runtime settings, and lifecycle hook collection order. | Parent T-0004 / r1 | Done | T-0005, T-0006 | Initiative / High | None | Stop; outcome complete. | `.meta/tasks/0007-feature-metadata-brief.md` | Added Feature `customizations.dcc.commands` and `state`, legacy `scripts` warnings, Feature state metadata/runtime merge, unsupported `init`/`entrypoint` warnings, Feature user-property errors, unsafe Feature runtime gating via `--allow-unsafe-runtime`, docs, and tests. Verified fmt, clippy, tests, and build. |
| T-0008 | Implement build preparation: official `build` source support, generated controller/hook assets, default prep hooks, and `build --refresh-only`. | Parent T-0004 / r1 | Done | T-0005, T-0006, T-0007 | Initiative / High | None | Stop; outcome complete. | `.meta/tasks/0008-build-prep-brief.md` | Added official `build` source support, image/build conflict validation, generated controller/wrapper/hook assets, build-preparation hook execution, `build --refresh-only`, docs, and tests. Verified fmt, clippy, tests, and build. |
| T-0009 | Implement durable runtime lifecycle commands: `start`, `stop`, `run`, `exec`, `attach`, one-shot bookkeeping, and `--keep` promotion. | Parent T-0004 / r1 | Done | T-0008 | Initiative / High | None | Stop; outcome complete. | `.meta/tasks/0009-runtime-lifecycle-brief.md` | Added `start`, `attach`, `--keep`/`-k`, durable promotion, existing-container reuse, one-shot active-command bookkeeping, scoped startup/attach hooks, docs, and tests. Verified fmt, clippy, tests, and build. |
| T-0010 | Complete port attributes, safe `runArgs`, documentation, fixtures, official config validation, strict review, and final parent closure. | Parent T-0004 / r1 | Done | T-0005, T-0006, T-0007, T-0008, T-0009 | Initiative / High | None | Stop; outcome complete. | `.meta/tasks/0010-final-validation-brief.md` | Added final compatibility parsing, conservative `runArgs`, devcontainer unsafe runtime gating, sensitive mount protections, `workspaceFolder` workdir support, docs, strict review fixes, and parent closure. Verified fmt, clippy, tests, build, and follow-up official devcontainer CLI config validation. |
| T-0011 | Install missing validation tooling and run official Dev Container CLI config validation for the completed rewrite. | User resume and sudo/tooling approval 2026-07-15 / r1 | Done | T-0004 | Quick change / Low | None | Stop; outcome complete. | `.meta/quality/0004-dcc-rewrite-quality.md`; `.meta/tasks/0004-dcc-rewrite-notes.md` | Installed Node.js v20.19.2, npm 9.2.0, `@devcontainers/cli 0.87.0`, and Docker 26.1.5; started a constrained Docker daemon for CLI inspection; `sudo devcontainer read-configuration --workspace-folder /workspace --include-merged-configuration --log-level trace` passed and produced merged configuration output. Ignored live Docker smoke was attempted but blocked by harness mount/namespace restrictions. |
| T-0012 | Enable Docker-dependent smoke tests in GitHub Actions while keeping local development-container tests Docker-free by default. | User request 2026-07-20 / r1 | Done | T-0011 | Quick change / Low | None | Stop; outcome complete. | None | Added a `docker-test` CI job on GitHub-hosted Ubuntu runners that verifies Docker availability and explicitly runs the ignored Docker smoke tests. Updated test comments and standards to record that local `cargo test` remains Docker-free while CI owns live Docker build/run coverage. Verified workflow syntax with `actionlint`, listed the ignored Docker tests without running them locally, and passed fmt, clippy, tests, and build. |
| T-0013 | Add Docker-free dry-run validation and JSON output for CLI acceptance tests. | User request 2026-07-20 / r1 | Done | T-0012 | Quick change / Medium | None | Stop; outcome complete. | None | Added global `--dry-run` and `--format text\|json` flags, stable dry-run reports, Docker-boundary exits for build/runtime/stop commands, JSON output for `dcc id`, docs, and CLI tests that use dry-run success instead of fragile Docker error parsing. Kept ignored Docker smokes for real build/exec/run coverage. Verified fmt, tests, clippy, build, help output, dry-run JSON output, ignored-test listing, and workflow lint with `actionlint 1.7.12`. |
| T-0014 | Add expanded Docker behavior smoke tests for the completed rewrite. | User request 2026-07-20 / r1 | Done | T-0012, T-0013 | Quick change / Medium | None | Stop; outcome complete. | None | Added ignored `tests/docker_smoke.rs` coverage for project named commands, lifecycle hook phase separation, state directory/file persistence across one-shot containers and refresh-only builds, durable vs one-shot container reuse, `workspaceFolder` workdir behavior, runtime environment substitution, and local Feature command/state metadata. Updated CI to run the suite serially on GitHub Docker runners. Verified fmt, tests, clippy, build, actionlint, and ignored-test listings locally; live Docker execution remains delegated to GitHub runners. |
| T-0015 | Update user-facing documentation to match the completed `dcc` rewrite design and implementation. | User request 2026-07-20 / r1 | Done | T-0004, T-0013, T-0014 | Quick change / Low | None | Stop; outcome complete. | None | Reworked the README around the current mental model, common workflows, `customizations.dcc` config, explicit build requirements, named `dcc run` commands versus direct `dcc exec`, one-shot/durable reuse, dry-run/JSON output, and documented differences from normal devcontainers including ignored compatibility fields, port forwarding, unsafe runtime gating, and unsupported top-level `remoteUser`. Verified `git diff --check`, stale README searches, and `cargo test`. |
| T-0016 | Remove `initializeCommand` execution and keep it parse-and-warn only. | User correction 2026-07-20 / r1 | Done | T-0004 | Quick change / Medium | None | Stop; outcome complete. | None | Removed host execution of `initializeCommand`, deleted the host lifecycle executor, excluded `initializeCommand` from runtime `${containerEnv:...}` probing and debug lifecycle output, preserved parser compatibility with an unsupported-field warning, and updated README/architecture docs. Added CLI coverage for the warning and Docker smoke coverage that would fail if the hook mutates the host workspace. Verified fmt, tests, clippy, diff check, and stale-reference search. |
| T-0017 | Reorganize README lifecycle-hook guidance into a dedicated trigger table. | User request 2026-07-20 / r1 | Done | T-0016 | Quick change / Low | None | Stop; outcome complete. | None | Moved lifecycle-hook behavior into its own README section, added a hook-by-hook table showing which `dcc` commands trigger or skip each hook, called out `dcc id`/`dcc stop`, and reduced duplicate hook behavior in the configuration table. Verified `git diff --check` and targeted lifecycle README searches. |
| T-0018 | Move shared README command flag guidance into a dedicated global flags subsection. | User request 2026-07-20 / r1 | Done | T-0017 | Quick change / Low | None | Stop; outcome complete. | None | Moved `--strict`, `--dry-run`, `--format json`, and launch debugging guidance into `Commands` > `Global flags`, removed the old dry-run and debugging subsections, and left command-specific sections focused on subcommand behavior. Verified `git diff --check` and targeted README heading/flag searches. |
| T-0019 | Make `--debug` a global CLI flag supported by all commands. | User request 2026-07-20 / r1 | Done | T-0018 | Quick change / Medium | None | Stop; outcome complete. | None | Moved `--debug` from runtime subcommands to the global CLI options, threaded it through existing runtime launch debugging, added build/stop/id debug output for resolved profile/config/container/image details, updated README and architecture docs, and added CLI tests for global placement plus non-runtime debug output. Verified fmt, tests, clippy, help output, grep checks, and diff check. |
| T-0020 | Design and implement a `feature` subcommand that accepts `--add`/`-a` and `--remove`/`-r` to add and remove features from the profile. | User request 2026-07-21 / r1 | Done | None | Quick change / Medium | None | Stop; outcome complete. | None | Added `dcc feature` with repeatable `--add`/`-a` and `--remove`/`-r`, Docker-free dry-run/debug/JSON output support, JSONC profile parsing with formatted JSON writes, docs, and integration coverage. Verified fmt, check, tests, clippy, build, diff check, help output, and dry-run JSON output. |
| T-0021 | Guard `customizations.dcc.state` against critical container paths, including the currently unguarded `/cache` self-nesting bug and `dcc`'s own `/usr/local/share/dcc` assets. | User request 2026-07-21 / r1 | Done | None | Quick change / Medium | None | Stop; outcome complete. | `.meta/tasks/0021-state-path-guards-brief.md` | Added two-tier reserved-path guards to `normalize_state_path` (subtree + exact), applied at config load and post-`${containerEnv:VAR}` resolution. Tier 1 blocks `/proc`, `/sys`, `/dev`, `/tmp`, `/run`, `/var/run`, `/var/lock`, `/boot`, `/bin`, `/sbin`, `/lib*`, `/usr/bin`, `/usr/sbin`, `/usr/lib*`, `/etc`, `/workspace/.dcc`, `/cache`, `/usr/local/share/dcc` (subtree). Tier 2 blocks `/usr`, `/var`, `/home`, `/root`, `/opt`, `/workspace`, `/srv`, `/mnt`, `/media` (exact only; children accepted). Errors name the path, matched reserved path, and an alternative. Updated README and architecture reserved-path docs. Verified fmt, clippy, tests (439 unit), and build. |
| T-0022 | Seed declared state from the image so Feature- and Dockerfile-installed content is not silently masked by empty bind mounts, and survives a wiped `.dcc`. | User request 2026-07-21 / r1 | Done | T-0021 | Initiative / High | None | Stop; outcome complete. | `.meta/tasks/0022-state-seeding-brief.md`; `.meta/decisions/0001-state-seeding-from-image.md` | Added `src/seed.rs`: `dcc build` hydrates declared state from the image via a one-shot container (state mounts off, host state root at `/dcc-seed`) copying with `tar` inside the container before build-prep hooks; a `dcc.seed` image label carries the resolved manifest; a host-side `.dcc/<profile>.seed.json` ledger records `seed_digest`/`build_id`; re-seed policy (match→skip, differ→preserve+warn, `--reseed-state`→overwrite); runtime guard on start/run/exec/attach warns on `build_id` mismatch and hydrates unrecorded entries; `--dry-run` reports planned seeding. Added `--reseed-state` CLI flag, `docker::run_to_completion`, and 8 ignored Docker smoke tests. Updated README and architecture. Verified fmt, clippy, tests, build, help output, and dry-run JSON. Live Docker execution delegated to CI. |
| T-0023 | Contain `dcc`'s host-side runtime bookkeeping so container-side code cannot reach it through the `/cache` mount. | Accepted in-scope finding 2026-07-21 / r1 | Superseded by T-0024 | None | Quick change / High | None | Stop; superseded by T-0024 which removes the host-side bookkeeping entirely. | `.meta/tasks/0023-cache-runtime-exposure-brief.md` | Superseded by T-0024. T-0024 relocates lifecycle ownership into an in-container PID 1 supervisor, eliminating the host-side bookkeeping this task was opened to contain. |
| T-0024 | Move container lifecycle ownership into an in-container PID 1 supervisor; remove host-side file-based runtime bookkeeping and the `mkdir` lock; add `dcc stop` graceful / `--now` / `--kill` variants. | Architecture decision 2026-07-21 / r1 | Done | None | Initiative / High | None | Stop; outcome complete. | `.meta/tasks/0024-supervisor-lifecycle-ownership-brief.md`; `.meta/tasks/0024-r1-supervisor-design.md` | Implemented in-container PID 1 lifecycle supervisor (`src/supervisor.rs`): POSIX sh scripts bind-mounted read-only from `.dcc/<profile>.rt/`; state in tmpfs `/run/dcc`; one-shot self-teardown on drain; `dcc stop` graceful/--now/--kill. Removed `src/runtime.rs`; reworked exec.rs, stop.rs, build.rs; removed dead dcc-controller/dcc-command assets; added docker::kill_container. Updated threat model, README, architecture. T-0023 superseded. Verified fmt, clippy (0 prod warnings), 506 tests, build. |
| T-0024-R1 | Research and design the supervisor: implementation language, CLI↔entrypoint↔supervisor message protocol, active-set and mode state machine, and `stop` graceful/`--now`/`--kill` semantics. Produce a design document for review. | Parent T-0024 / r1 | Done | None | Design / High | None | Stop; outcome complete. | `.meta/tasks/0024-r1-supervisor-design.md` | Design document produced. |
| T-0024-S1 | Implement the PID 1 supervisor and command entrypoint: active-set state machine, mode handling, graceful-stop gating, and `--now` shutdown-hook execution; generate as in-container assets. | Parent T-0024 / r1 | Done | T-0024-R1 | Initiative / High | None | Stop; outcome complete. | None | Implemented `src/supervisor.rs` with 8 unit tests. |
| T-0024-H1 | Rework the host CLI (`src/exec.rs`, `src/stop.rs`, `src/docker.rs`) to drive the supervisor through the entrypoint; remove `src/runtime.rs` and all host-side bookkeeping file I/O; add `dcc stop` graceful / `--now` / `--kill` variants. | Parent T-0024 / r1 | Done | T-0024-S1 | Initiative / High | None | Stop; outcome complete. | None | Reworked exec/stop; deleted runtime.rs; added stop variants + kill_container; 3 CLI tests. |
| T-0024-B1 | Update the build-preparation container (`src/build.rs`) and generated controller/command assets (`src/features/mod.rs`) to use the supervisor as PID 1. | Parent T-0024 / r1 | Done | T-0024-S1 | Quick change / Medium | None | Stop; outcome complete. | None | Updated build_prep_container_args; removed dead dcc-controller/dcc-command assets. |
| T-0024-T1 | Add tests: supervisor state-machine unit tests, entrypoint mediation tests, `stop` variant behavior, and ignored Docker smoke tests asserting no host-side bookkeeping exists and teardown/reuse/stop behave correctly. | Parent T-0024 / r1 | Done | T-0024-H1, T-0024-B1 | Quick change / Medium | None | Stop; outcome complete. | None | 8 supervisor unit tests; 3 CLI tests; 5 ignored Docker smoke tests. |
| T-0024-D1 | Update `.meta/threat-models/0004-dcc-runtime.md`, README, and `.meta/project/architecture.md` to reflect supervisor ownership and the removed trust boundary; close T-0023 as superseded. | Parent T-0024 / r1 | Done | T-0024-H1 | Quick change / Low | None | Stop; outcome complete. | None | Updated architecture.md, README, threat model; T-0023 marked superseded. |
| T-0025 | Move startup sequencing and runtime lifecycle hooks into the supervisor: `--mode`/`--expect-command` entrypoint args, supervisor-run `postStartCommand`, a `dcc-ctl wait-ready` readiness probe the command harness blocks on after registering, and deletion of the time-based startup grace. | User direction 2026-08-13 / r2 | Done | T-0024 | Initiative / High | None | Stop; outcome complete. | `.meta/tasks/0025-supervisor-startup-handshake-brief.md`; `.meta/tasks/0025-r1-startup-handshake-design.md` | Implemented per the r3 design: supervisor accepts `--mode`/`--expect-command`/`--start-hooks` entrypoint args (retired `DCC_MODE` env); runs `postStartCommand` hooks (feature-first, then devcontainer) inside PID 1 from host-generated pre-substituted scripts in `.dcc/<profile>.rt/start-hooks/`; writes a single `/run/dcc/bootstrap-status` file (`0` or `<exit-code> <hook-name>`); `dcc-ctl wait-ready` uses a per-waiter FIFO handshake (mkfifo before status check, `<>` open to avoid wedging on orphaned FIFOs) — no polling, no package dependency; `dcc-exec` registers then waits for readiness before running the command; deleted `STARTUP_GRACE_SECS`/`PRIMED`/grace branch, replaced with a supervisor-local `arrived` variable + 10 s one-shot post-bootstrap orphan reaper; `postAttachCommand` stays host-side with a cold-start `wait-ready`; host maps exit 252 to a clear hook-failure error. `${containerEnv:…}` substitution stays host-side (no config-compatibility change). Updated `src/supervisor.rs` (full rewrite of all three scripts + hook-script generation + `sh_quote` + `RtDir::write_start_hooks`/`has_start_hooks`), `src/exec.rs` (entrypoint args, start-hook emission, `wait_ready`, removed `run_runtime_hooks(Startup)`), `src/build.rs` (`--mode durable` entrypoint arg). 478 unit tests (incl. 6 real-`sh` execution tests for bootstrap/failure/FIFO/fast-path/252), 33 ignored Docker smokes (9 new: cold-start ordering, slow hook, failing hook, start+stop variants, skip-lifecycle, drain, durable reuse). Verified fmt, clippy (0 warnings incl. `--all-targets`), test, build. Live Docker execution delegated to CI. Fast-path removal split out as T-0027 (not a prerequisite). |
| T-0026 | Implement the spec's `updateRemoteUserUID` (default `true`, Linux-only) so a non-root `containerUser` can write to bind-mounted workspace, cache, and state regardless of host uid; restore `seeded_directory_is_writable_by_container_user` to its original workspace-writing form. | User direction 2026-08-13 / r1 | Done | None | Initiative / High | None | Stop; outcome complete. | `.meta/tasks/0026-update-remote-user-uid-brief.md`; `.meta/tasks/0026-r1-uid-remap-design.md` | Added `updateRemoteUserUID` (recognized config field, default `true`, child-overrides-parent merge) and `src/uid.rs`: `plan_uid_remap` with every reference no-op (root, numeric, already-matching, uid collision, gid-collision-keeps-gid, non-Linux, disabled, host-ids-unavailable), `host_ids` via `id -u`/`id -g`, and `remap_dockerfile_block`/`remap_build_args` baking the `devcontainers/cli` `updateUID.Dockerfile` logic into dcc's generated build stage as a `RUN` after user creation with `--build-arg REMOTE_USER/NEW_UID/NEW_GID`. Wired into `build_dcc_stage` (`docker::build` now forwards build args), `--debug`, and `--dry-run` reports. Restored `seeded_directory_is_writable_by_container_user` to `/workspace/writable.txt` + `fx.read_file`; added ignored Docker smoke tests for non-root writes to `/workspace`/`/cache`/state, in-container uid matching host, and root fast-path unaffected. Updated README and architecture. Seeding interaction resolved: the remap is baked at image build, so T-0022 hydration already sees the remapped `/etc/passwd` and seeded content is owned by the remapped user. Verified fmt, clippy (0 warnings), 463 unit tests, build, dry-run JSON. Live Docker execution delegated to CI. |
| T-0027 | Remove the `image` fast path (`uses_fast_path`) so every dcc container is uniformly built from a dcc image stage, and drop the fast-path special cases in `build.rs`, `version.rs`, `run.rs`, `stop.rs`, `exec.rs`, and `uid.rs`. | User direction 2026-08-13 / r2 | Done | None | Initiative / Medium | None | Stop; outcome complete. | `.meta/tasks/0027-remove-image-fast-path-brief.md`; `.meta/decisions/0003-remove-image-fast-path.md` | Deleted `uses_fast_path` and its pull+tag branch in `build.rs`; every build now runs `build_base_image` + `build_dcc_stage`, so every image carries a `dcc.version` label. C1: added `pull: bool` to `DockerBuildOptions`, emit `--pull` in `build_args` when set, plumbed `opts.update`→`pull` through `build_base_image` (build-source path) and `build_dcc_stage` (dcc stage); `--update` help text documents the base-image re-pull. C2: `write_lockfile` skips the write when `lock_entries` is empty and no lockfile exists (an existing lockfile is still rewritten). C3: deleted `docker::pull`/`docker::tag` (fast path was their only caller). Dropped `current_uses_fast_path` param from both `version.rs` entry points; `version_warning` collapses three `None` arms into one (missing label now always warns). Removed fast-path plumbing in `exec.rs`, `run.rs`, `stop.rs` (incl. the local `current_uses_fast_path` helper), and the vacuous `fast_path_config_implies_no_remap` test in `uid.rs`. `--no-cache` now honored for all profiles (fast path used to discard it). Deleted 10 tests (7 fast-path + 2 version + 1 uid), added 2 `--pull` flag tests. Updated docker_smoke `fast_path_root_profile_unaffected_by_uid_remap`→`root_image_profile_builds_with_version_stamp_and_no_remap` (now asserts the version label). Updated README + architecture fast-path prose. 470 unit tests, 29 integration, 33 ignored Docker smokes; fmt, clippy (`--all-targets -D warnings`), check, test, build all clean. `rt` bind mount unchanged (T-0028 tracks the delivery-model question). |
| T-0028 | Reconsider the `rt` bind mount and supervisor delivery model now that removing the fast path lifts the constraint that forced it; decide between the read-only bind mount, baking assets into the image, or a hybrid, and record the outcome. | User direction 2026-08-13 / r1 | Done | T-0027 | Design / Medium | None | Stop; outcome complete. | `.meta/tasks/0028-supervisor-delivery-model-brief.md`; `.meta/decisions/0004-embed-supervisor-in-image.md` | Decided: **bake the supervisor scripts into the image, keep `postStartCommand` hook scripts on the `rt` bind mount**, and add a semver compatibility gate. Q1: version skew handled by interpreting the `dcc.version` label with semver rules (patch = compatible, major/minor or missing label = refuse) instead of the current warning; patch releases may not change the host↔supervisor protocol. Q2: root-user tampering with baked scripts declared an explicit non-goal; the read-only mount property was found not load-bearing for the threat model. Q3 (decisive): hooks cannot be baked because `apply_substitutions` resolves `${localEnv:VAR}` in `lifecycle` from the invoking process's env, so hook text is only fully resolvable at run time — the codebase already encodes this rule by denying `localEnv` to `containerEnv`, which *is* baked. Also found that the baked build-prep `.sh` assets are never executed (the host `docker exec`s a freshly substituted argv), so they are vestigial. Q4/Q5/Q6 excluded from the evaluation per owner direction. Corrected stale rationale in T-0024 D1 and T-0025 D0/D2; updated the threat model scope and added an accepted-risk row. Implementation split to T-0029 and T-0030. |
| T-0029 | Bake the supervisor scripts (`dcc-supervisor`, `dcc-ctl`, `dcc-exec`) into the image via the build context, retarget the entrypoint and exec paths off the `rt` mount, shrink `RtDir` to manage only `start-hooks/`, and handle the build-prep container's now-possibly-empty mount. | Decision 0004 (T-0028) / r1 | Done | T-0028, T-0030 | Initiative / Medium | None | Stop; outcome complete. | `.meta/decisions/0004-embed-supervisor-in-image.md` | Added `baked_supervisor_assets()` to `src/supervisor.rs` returning the three scripts as `.dcc-generated/` context files; `generated_assets()` in `src/features/mod.rs` now includes them so `COPY .dcc-generated/ /usr/local/share/dcc/` bakes them (always-on). Added `DCC_SHARE` const; retargeted `--entrypoint`, `dcc-ctl`, `dcc-exec` paths from `RT_MOUNT/dcc-*` to `DCC_SHARE/dcc-*` in `exec.rs`, `stop.rs`, `build.rs`, and the generated shell scripts. `RtDir::materialize` no longer writes the three scripts — only creates the dir and clears stale `start-hooks/`; the `rt` bind mount survives for `start-hooks/` only. Updated supervisor.rs docs, exec.rs debug output, and 3 supervisor tests. 479 unit tests; fmt, clippy, test, build clean. |
| T-0030 | Replace the `dcc.version` mismatch warning with a semver compatibility gate for runtime commands (patch-level drift proceeds; major/minor drift or a missing label refuses with the rebuild command), and record the "no host↔supervisor protocol change in a patch release" rule in project standards. | Decision 0004 (T-0028) / r1 | Done | T-0028 | Quick change / Medium | None | Stop; outcome complete. | `.meta/decisions/0004-embed-supervisor-in-image.md` | Rewrote `src/version.rs`: replaced `warn_if_image_version_mismatch`/`_best_effort`/`version_warning` with `ensure_image_version_compatible`/`_best_effort` and a `SemVer` type (no new dependency — minimal `major.minor.patch` parser that strips `v` prefix and ignores pre-release/build metadata). Compatibility: equal or patch-only drift proceeds; major/minor drift or missing label returns an error naming the rebuild command. `dcc build` is exempt. Updated callers in `exec.rs`, `run.rs`, `stop.rs`. Added 10 semver tests. Recorded the patch-stable-protocol rule in `.meta/project/standards.md`. 479 unit tests; fmt, clippy, test, build clean. |
| T-0031 | Remove the vestigial build-prep hook `.sh` assets baked into the image at `/usr/local/share/dcc/hooks/build-prep/`; they are never executed because the host `docker exec`s a freshly substituted argv via `run_planned_hooks`/`run_in_container`. | Discovered in-scope finding during T-0028 / r1 | Done | None | Quick change / Low | None | Stop; outcome complete. | None | Deleted `build_prep_hook_assets`, `hook_asset`, `lifecycle_script`, `shell_command`, `shell_quote`, and `asset_slug` from `src/features/mod.rs`; `generated_assets()` now takes no arguments and returns only `baked_supervisor_assets()`. Removed the dead `feature_build_prep_hooks` collection. Replaced the build-prep asset test with `generated_assets_returns_only_supervisor_scripts`. Updated the reserved-path test to reference `dcc-supervisor`. 479 unit tests; fmt, clippy, test, build clean. |
| T-0033 | Relocate everything under the `readme` directory to `.meta` and make every internal link and reference consistent with the new path. | User request 2026-08-13 / r1 | Done | None | Quick change / Low | None | Stop; outcome complete. | None | `git mv readme .meta` (62 files, rename-detected, history preserved), then rewrote every `readme/` path reference to `.meta/` across the 44 files that carried one: all of `.meta/**`, root `AGENTS.md`, and two Rust doc comments (`src/features/context.rs`, `src/seed.rs`). Preserved five historical mentions of the pre-framework `readme/DEVELOPMENT.md`, `readme/STYLE.md`, `readme/ARCHITECTURE.md`, and the unexpected-`readme/`-files finding in T-0002/T-0003 rows and source-map S-0005/S-0006/S-0007 provenance, because those named files never existed at `.meta`. The framework core keeps its `<root>/meta/` contract, so the reusable tree is now `.meta/meta/`. Verified every relative Markdown link in `.meta`, `AGENTS.md`, and `README.md` resolves (0 broken); the only unresolved backtick paths are pre-existing `NNNN-` schema placeholders and not-yet-created optional artifacts. No references exist in `.gitignore`, `.github/`, `.devcontainer/`, `scripts/`, `install.sh`, `Cargo.toml`, or the product `README.md`. Verified `cargo fmt --check`, `cargo check`, `cargo clippy -- -D warnings`, and `cargo test` (479 unit, 29 CLI, 9 config, 6 feature; 33 Docker smokes still ignored). |
| T-0032 | Decide whether `postStartCommand` hook scripts should be baked into the image alongside the supervisor (T-0029) or remain host-generated on the `rt` bind mount as T-0028 provisionally concluded; the blocker is `${localEnv:VAR}`, which is only resolvable at run time from the invoking user's environment. | User direction during T-0028 closure / r1 | Done | T-0028 | Design / Medium | None | Stop; outcome complete. | `.meta/tasks/0032-whether-to-bake-hooks-brief.md`; `.meta/decisions/0004-embed-supervisor-in-image.md` | Decision 0004 already answers Q1–Q4 and T-0029 implemented it: startup hooks remain host-generated on the `rt` mount so `${localEnv:…}` stays runtime-correct; baking or a second in-container substitution engine remains rejected. The 2026-08-24 maintenance pass corrected the stale catalog status. |
| T-0034 | Diagnose the latest CI failure from `logs_86711534614.zip` and fix the underlying issue. | User request 2026-08-17 / r1 | Done | None | Quick change / Medium | None | Stop; outcome complete. | None | CI logs showed the Docker smoke job failed in `exec_runs_direct_command` because `docker run` parsed supervisor `--mode` as a Docker flag. Fixed runtime and build-prep argv construction so the image precedes supervisor entrypoint args, and added regression coverage. Verified focused tests, `cargo fmt --check`, `cargo clippy -- -D warnings`, `cargo test` (480 unit, 29 CLI with 3 ignored, 9 config, 6 feature; 33 Docker smokes still ignored), `cargo build`, and `git diff --check`. Exact ignored Docker smoke was not runnable locally because `docker` is not installed. |
| T-0035 | Update the commit trailer requirement to name the actual model and harness used for the work, and correct the latest commit's inaccurate Claude trailer. | User request 2026-08-17 / r1 | Done | None | Quick change / Low | None | Stop; outcome complete. | None | Updated `.meta/project/development.md` to require a `Co-Authored-By` trailer naming the actual model and coding harness, using `Codex GPT-5 <noreply@openai.com>` only as the example for this session. Verified no stale `Claude Opus 4.8`/Anthropic trailer remains in the project guidance and `git diff --check` passed. Amended `HEAD` to use the Codex GPT-5 trailer. |
| T-0036 | Diagnose the latest CI failure from `logs_86723951441.zip` and fix the remaining Docker smoke regressions. | User request 2026-08-17 / r1 | Done | None | Quick change / Medium | None | Stop; outcome complete. | None | CI logs showed the Docker smoke job now passed the earlier `exec_runs_direct_command` case but failed four smokes: generated startup hooks masked `exit 42` because they relied on non-POSIX `PIPESTATUS`; seeded non-root state remained owned by the pre-remap uid; and one smoke invoked `dcc exec --keep run check` instead of named-command `dcc run --keep check`. Fixed hook status preservation under POSIX `sh`, re-owned hydrated state to non-root `containerUser` after tar extraction, corrected file-state hydration parent creation, updated the durable reuse smoke, and refreshed ownership docs/decision notes. Verified focused hook and seed tests, `cargo fmt --check`, `cargo clippy -- -D warnings`, `cargo test` (481 unit, 29 CLI with 3 ignored, 9 config, 6 feature; 33 Docker smokes still ignored), `cargo build`, stale-doc searches, and `git diff --check`. Exact ignored Docker smokes were not runnable locally because `docker` is not installed. |
| T-0037 | Diagnose the latest CI failure from `logs_86726174884.zip` and fix the underlying issue. | User request 2026-08-17 / r1 | Done | None | Quick change / Medium | None | Stop; outcome complete. | None | CI logs showed the Docker smoke job had one remaining failure: `durable_and_one_shot_container_modes_behave_differently` observed a one-shot `dcc exec` container still running immediately after the command returned. Fixed foreground execution to track whether the invocation created the container, wait only for newly created one-shot containers, and use the same label-based running-container query as reuse detection and the smoke assertion. Added Docker-free unit coverage for the teardown-wait predicate. Verified `cargo fmt --check`, `cargo clippy -- -D warnings`, `cargo test` (482 unit, 29 CLI with 3 ignored, 9 config, 33 Docker smokes ignored, 6 feature), `cargo build`, and `git diff --check`. Exact ignored Docker smokes were not runnable locally because `docker` is not installed. |
| T-0038 | Diagnose the latest CI failure from `logs_86727797288.zip` and fix the underlying issue. | User request 2026-08-17 / r1 | Done | None | Quick change / Medium | None | Stop; outcome complete. | None | CI logs showed `durable_and_one_shot_container_modes_behave_differently` still failed after the host-side teardown wait: a very fast one-shot command could register and deregister entirely between supervisor polling intervals, so PID 1 never observed `active_count > 0`, left `arrived=0`, and waited for the 10 s orphan reaper. Fixed `dcc-exec` to write a persistent `$STATE/arrived` marker after creating the active record, and taught the supervisor to treat that marker as command arrival. Added a real `/bin/sh` regression test that runs the generated supervisor and `dcc-exec /bin/true` together and asserts one-shot drain without waiting for the orphan reaper. Verified focused supervisor tests, `cargo fmt --check`, `cargo clippy -- -D warnings`, `cargo test` (483 unit, 29 CLI with 3 ignored, 9 config, 33 Docker smokes ignored, 6 feature), `cargo build`, and `git diff --check`. Exact ignored Docker smokes were not runnable locally because `docker` is not installed. |
| T-0039 | Reorganize public documentation: keep `README.md` as an end-user overview, move feature detail to `docs/features.md`, move development guidance to `docs/development.md`, and link those pages from the README. | User request 2026-08-17 / r1 | Done | None | Quick change / Low | None | Stop; outcome complete. | None | Added `docs/features.md` and `docs/development.md`, shortened `README.md` to an end-user overview with links to both docs, moved detailed feature/configuration/runtime guidance into the feature guide, moved maintainer/release guidance into the development guide, and corrected stale baked-supervisor wording in public docs plus `.meta/project/architecture.md`. Verified Markdown local links/anchors, `git diff --check`, `cargo fmt --check`, `cargo clippy -- -D warnings`, `cargo test`, and `cargo build`. |
| T-0040 | Reorganize public docs again: move the broad usage guide from `docs/features.md` to `docs/index.md`, create a new `docs/features.md` focused on devcontainer Feature packages, and link it to the official Features documentation. | User request 2026-08-17 / r1 | Done | None | Quick change / Low | None | Stop; outcome complete. | None | Moved the broad guide to `docs/index.md`, created a new `docs/features.md` focused on devcontainer Feature packages with a link to the official Features reference, moved Feature-package details out of the general guide, updated README and development guide links, and updated project doc ownership notes. Verified Markdown local links/anchors, `git diff --check`, `cargo fmt --check`, `cargo clippy -- -D warnings`, `cargo test`, and `cargo build`. |
| T-0041 | Remove lockfile implementation because Feature-backed installs are not version-locked enough for lockfiles to add much value. | User request 2026-08-24 / r1 | Done | None | Quick change / Medium | None | Stop; outcome complete. | None | Removed `devcontainer.lock` read/write behavior, lock entry generation, locked OCI digest plumbing, and local Feature digest storage; OCI downloads still verify the resolved blob digest fetched from the manifest. Updated `--update` help and docs to describe base-image refresh only. Verified `cargo check`, stale lockfile reference scan, `cargo fmt --check`, `cargo clippy -- -D warnings`, `cargo test` (483 unit, 29 CLI with 3 ignored, 9 config, 33 Docker smokes ignored, 6 feature), and `cargo build`. |
| T-0042 | Fully remove the `dcc build --update` flag and make `--no-cache` also re-pull base images. | User request 2026-08-24 / r1 | Done | None | Quick change / Medium | None | Stop; outcome complete. | None | Removed the `build --update` CLI flag, dispatch field, and build option plumbing. `--no-cache` now passes Docker `--pull` when building from an upstream base image: directly for image profiles and during the official `build` source's base-image build, while avoiding `--pull` for the generated local intermediate base tag. Updated docs and architecture, and added CLI coverage that `--update` is rejected. Verified `cargo check`, `cargo fmt --check`, `cargo clippy -- -D warnings`, `cargo test` (483 unit, 30 CLI with 3 ignored, 9 config, 33 Docker smokes ignored, 6 feature), `cargo build`, stale `--update` scan, and `git diff --check`. |
| T-0043 | Coordinate the test-quality corrections identified by the 2026-08-24 suite audit so tests protect user-visible behavior, compatibility, and security rather than transient implementation details. | User follow-up to test audit 2026-08-24 / r1 | Done | Child tasks T-0044 through T-0053 | Initiative / High | None | Stop; outcome complete. | `.meta/tasks/0043-test-quality-corrections-brief.md`; `.meta/tasks/0043-test-quality-corrections-notes.md`; `.meta/quality/0043-test-quality-corrections-quality.md` | Completed all ten children in task-isolated commits. Corrected Feature trace secrecy, OCI trust handling, Linux UID gating, anonymous volumes, forwarding lifecycle, smoke fidelity, CLI-to-Docker boundaries, and `containerEnv` compatibility; pruned brittle tests and added bounded property/behavior matrices. Aggregate format, check, all-target clippy, full test (502 unit, 30 runnable CLI with 3 ignored, 9 config, 8 fake-Docker, 9 Feature CLI, and 32 ignored Docker smokes), build, help, diff, security, consistency, and scheduled hygiene reviews passed. Live Docker smokes were not runnable because no `docker` executable exists; CI explicitly owns all 35 ignored Docker-backed cases. |
| T-0044 | Remove unconditional shell tracing from Feature installation and replace trace-shape tests with coverage that protects install behavior without exposing secrets. | Parent T-0043; user follow-up 2026-08-24 / r1 | Done | None | Quick change / High | None | Stop; outcome complete. | `.meta/tasks/0044-feature-install-tracing-brief.md` | Removed `inject_trace` and now archives Feature `install.sh` bytes unchanged. Replaced trace-shape tests with archive byte-preservation, POSIX/Bash/shebang-less execution, and no-secret-output coverage; a deliberate `set -x` negative control proves the secret assertion catches the prior behavior. Focused 35 tests, clippy, full test (480 unit plus integration suites), build, format, diff, and secret-output review passed. |
| T-0045 | Make `updateRemoteUserUID` behavior Linux-only as specified and add host-independent tests for Linux and non-Linux platform decisions. | Parent T-0043; user follow-up 2026-08-24 / r1 | Done | None | Quick change / Medium | None | Stop; outcome complete. | `.meta/tasks/0045-uid-platform-contract-brief.md` | Replaced the Unix-wide gate with an injected Linux platform decision and restricted `host_ids()` to `target_os = "linux"`. Deterministic tests now prove simulated Linux remaps and simulated macOS/Windows no-ops, while disabled/root/numeric/unavailable-ID and in-image collision/already-matching coverage remains. Replaced the no-panic host smoke with observed numeric UID/GID assertions and reconciled architecture, T-0026 design, and quality evidence. Focused 13 tests, format, clippy, full test (492 unit plus integration suites), build, and diff review passed. |
| T-0046 | Correct anonymous Feature volume serialization and test the Docker mount contract for anonymous and named volumes. | Parent T-0043; user follow-up 2026-08-24 / r1 | Done | None | Quick change / Medium | None | Stop; outcome complete. | `.meta/tasks/0046-anonymous-volume-contract-brief.md` | `FeatureMount::to_mount_string` now emits anonymous volumes as `type=volume,target=<path>` with no empty source field, while named volumes and bind mounts retain their source. Direct serialization and parsed runtime-label tests cover all three forms; the corrected anonymous assertion fails against the prior always-present formatting. Focused tests, format, clippy, full test (494 unit plus integration suites), build, diff review, and final `--mount` propagation inspection passed. |
| T-0047 | Add meaningful coverage for port forwarding, including bind failures, bidirectional relay behavior, shutdown, and IPv4/IPv6 availability. | Parent T-0043; user follow-up 2026-08-24 / r1 | Done | None | Initiative / High | None | Stop; outcome complete. | `.meta/tasks/0047-port-forwarding-coverage-brief.md` | Relay startup now binds all requested IPv4 listeners before spawning, treats IPv6 loopback as an optional family, retains connection tasks for cancellation, kills connectors on drop, and joins listener shutdown. The copy contract half-closes each completed output while draining the other direction. Six bounded deterministic tests cover bidirectional bytes and response drain, later-port collision rollback, IPv6 degradation, connector failure, IPv4 startup, and active-task cancellation; five consecutive focused runs, format, and clippy passed. Added an ignored real `dcc exec`/Docker/`nc` round-trip smoke; it compiled but could not run because Docker is unavailable in this environment. |
| T-0048 | Test and harden the OCI Feature acquisition trust boundary, including authentication, HTTP failures, digest verification, metadata validity, and archive safety. | Parent T-0043; user follow-up 2026-08-24 / r1 | Done | None | Initiative / High | None | Stop; outcome complete. | `.meta/tasks/0048-feature-acquisition-trust-tests-brief.md` | Added a deterministic local HTTP registry fixture covering reordered/optional Bearer challenges, URL-encoded token acquisition, token reuse, sanitized auth/manifest/blob failures, and a valid end-to-end acquisition; production token realms must use HTTPS. Centralized strict SHA-256 verification with a one-byte mutation control; malformed and schema-invalid supplied metadata now error while missing metadata remains documented install-only compatibility; archive extraction rejects traversal, absolute paths, links, and special entries. Focused trust-boundary tests, repeat fixture runs, format, clippy, full test (491 unit plus integration suites), build, diff, docs, and security review passed. |
| T-0049 | Replace false-confidence CLI and Docker smoke assertions with direct checks of success, identity, reuse, teardown, and container state; keep Docker-free dry-run tests in the default suite. | Parent T-0043; user follow-up 2026-08-24 / r1 | Done | None | Initiative / Medium | None | Stop; outcome complete. | `.meta/tasks/0049-integration-smoke-fidelity-brief.md` | Dry-run reports now expose the resolved container id. Path-profile integration tests require successful JSON output and compare the exact id across `id`, build, and stop. Seeding dry-run moved into the default suite and succeeds with an executable-free `PATH`; Docker fixture cleanup tolerates a missing Docker binary. Live smokes query label-selected immutable Docker instance IDs: immediate stop variants prove absence, one-shot drain captures and proves removal of the original instance without a fixed acceptance sleep, and durable reuse/`--keep` preserve identity plus a single-run hook counter. Retired host-bookkeeping assertions were removed. Format, clippy, full test (500 unit, 30 CLI plus 3 ignored, 9 config, 32 compiled Docker smokes, 6 feature), build, smoke listing, name/assertion review, and diff check passed; live smokes were not runnable because Docker is unavailable. |
| T-0050 | Add cross-layer tests proving runtime compatibility gates and CLI build/resource flags reach Docker with the correct arguments and conditions. | Parent T-0043; user follow-up 2026-08-24 / r1 | Done | None | Initiative / Medium | None | Stop; outcome complete. | `.meta/tasks/0050-cli-docker-contract-tests-brief.md` | Added `tests/docker_boundary.rs`, an argv-recording stateful fake Docker executable that drives the compiled CLI. Eight tests prove missing and major/minor-incompatible version labels refuse startup with the right rebuild instruction, patch drift reaches container creation, stop remains best-effort, `--no-cache` pulls image-profile and official upstream bases but not the generated intermediate, and explicit memory/CPU values reach `docker run` before image arguments. To satisfy the unspecified case, runtime resource flags are now optional and omitted from Docker unless supplied; docs and architecture no longer claim implicit 4g/2 limits. Focused fake-boundary and CLI tests, help output, format, clippy, full test (500 unit plus 8 fake-Docker and other integration suites), build, ordering review, and diff check passed. |
| T-0051 | Resolve the contradictory undefined `containerEnv` contract, then align implementation, public documentation, and tests around the chosen behavior. | Parent T-0043; user follow-up 2026-08-24 / r1 | Done | None | Decide / Medium | None | Stop; outcome complete. | `.meta/tasks/0051-container-env-contract-brief.md`; `.meta/decisions/0005-container-env-substitution.md` | Adopted the upstream absent/default contract: an absent variable uses its default or empty, a present empty value stays empty, and a present non-empty value wins. State-path validation still runs after substitution, and lifecycle's fallible mapping still permits consumer validation. Aligned implementation, public docs, architecture, source map, and named counterfactual tests. Format, clippy, full test (504 unit plus integration suites), build, and diff check passed. |
| T-0052 | Remove or rewrite redundant and implementation-coupled tests whose assertions do not protect a stable user, compatibility, or safety property. | Parent T-0043; user follow-up 2026-08-24 / r1 | Done | None | Quick change / Medium | None | Stop; outcome complete. | `.meta/tasks/0052-prune-brittle-tests-brief.md`; `.meta/tasks/0043-test-quality-corrections-notes.md#t-0052-candidate-classification` | Classified every named candidate. Deleted nine duplicate, retired-symbol, exact-layout, or misleading tests; rewrote supervisor assets, root/version-label behavior, and Feature-before-netcat ordering around stable outcomes. Positive stop dry-run and real-shell supervisor tests retain user-observable coverage. Focused modules, format, clippy, full test (495 unit plus integration suites), build, diff check, and coverage mapping passed. |
| T-0053 | Add high-leverage property and behavior-matrix tests for config merging, shell quoting, Dockerfile ordering, seed digests, and Feature command editing. | Parent T-0043; user follow-up 2026-08-24 / r1 | Done | None | Initiative / Medium | None | Stop; outcome complete. | `.meta/tasks/0053-property-and-matrix-tests-brief.md` | Added fixed-seed bounded merge generators spanning scalar, build, map, list, Feature, lifecycle, and nested dcc families, with two-sided identity and full precedence/union/order laws; an inverted-precedence mutation failed and shrank after three cases. Shell quoting now round-trips an edge matrix and 96 bounded ASCII cases through `/bin/sh`. Build-context coverage requires the stage, version label, Feature and generated assets, plus Feature-before-netcat order. Seed digest tests prove content/path/symlink sensitivity, creation-order stability, and intentional permission-mode normalization, now documented. Feature CLI tests cover JSONC combined edits, duplicate coalescing, and byte-preserving no-op/missing removal summaries. Format, clippy, full test (502 unit, 9 Feature CLI, and other integration suites), build, and diff check passed. |
| T-0054 | Implement `updateRemoteUserUID` mapping on macOS while preserving the established Linux behavior and explicit Windows no-op. | User request 2026-08-24 / r1 | Done | T-0026, T-0045 | Initiative / High | None | Stop; outcome complete. | `.meta/tasks/0054-macos-uid-remap-brief.md`; `.meta/quality/0054-macos-uid-remap-quality.md` | macOS now discovers host UID/GID and plans the existing safeguarded build-stage remap; Linux behavior is unchanged, and Windows/unsupported hosts explicitly no-op. Deterministic platform tests, focused UID/Dockerfile tests, format, check, clippy, the full test suite, build, and diff checks passed. A live macOS Docker Desktop run remains environment-bound residual evidence. |
| T-0055 | Restore implicit Docker runtime limits of 4 GiB memory and 2 CPUs, retain explicit overrides, and document the defaults as intended product behavior. | User request 2026-08-24 / r1 | Done | T-0050 | Quick change / Medium | None | Stop; outcome complete. | `.meta/tasks/0055-default-docker-limits-brief.md`; `.meta/quality/0055-default-docker-limits-quality.md` | `run`, `exec`, `attach`, and `start` now create containers with default `--memory 4g --cpus 2`; either explicit option overrides only that resource. CLI help and docs expose the defaults, and fake-Docker tests cover all entry points, ordering, uniqueness, and partial overrides. Format, check, clippy, full tests, build, and diff checks passed; live Docker enforcement remains environment-bound residual evidence. |
| T-0056 | Restore an error for missing `${containerEnv:VAR}` values without a default and document the intended substitution contract. | User request 2026-08-24 / r1 | Done | T-0051 | Initiative / Medium | None | Stop; outcome complete. | `.meta/tasks/0056-missing-container-env-error-brief.md`; `.meta/decisions/0006-require-missing-container-env-default.md`; `.meta/quality/0056-missing-container-env-error-quality.md` | Missing `${containerEnv:VAR}` now errors unless an explicit default is supplied; explicit empty defaults remain allowed, present-empty stays empty, and present-nonempty wins. Fallible resolution with consumer context covers state, workspaceFolder, runArgs, command arguments, mounts, remoteEnv, build/runtime lifecycle hooks, and Feature equivalents. Decision 0006 supersedes 0005. Format, check, clippy, full tests, build, and diff checks passed. |
| T-0057 | Fully design and implement `dcc profile list` so users can discover available profiles, including stable text and JSON output, documentation, and automated verification. | User request 2026-08-24 / r1 | Done | None | Initiative / Medium | None | Stop; outcome complete. | `.meta/tasks/0057-profile-list-brief.md`; `.meta/quality/0057-profile-list-quality.md` | Added nested `dcc profile list` discovery of direct selectable `.devcontainer/*.json` files, deterministic sorting, default annotation, filename-safe one-record-per-line text, and stable JSON records with name/config/default. Listing is read-only, Docker-free, works from nested directories, and bypasses irrelevant selected-profile resolution. Unit and CLI matrices cover filtering, symlinks, non-UTF-8/control names, empty results, output contracts, debug, and help. Format, check, all-target Clippy, 516 unit tests plus all runnable integration suites, build, manual output/help, docs, security, and diff reviews passed. |
| T-0058 | Diagnose the build issue evidenced by the latest CI logs in `logs_88701649099.zip`. | User request 2026-08-24 / r1 | Done | None | Discover / Medium | None | Stop; diagnosis complete and no product-code change was authorized. | None | CI checkout `24d35e5` passed format, Clippy, ordinary tests, and build; only `forwarded_port_reaches_container_loopback_service` failed because it ended with an empty response after its read deadline. The relay correctly half-closes the connector's stdin, but its `docker exec -i … nc 127.0.0.1 PORT` command omits OpenBSD netcat's `-N`, so stdin EOF is not propagated as a TCP write-half close. A Docker-free reproduction showed the exact connector delivered both directions but timed out, while adding `-N` completed immediately. All six forwarding unit tests passed, confirming their fake upstream does not cover the real netcat process contract. Live Docker reproduction was unavailable because this nested environment denies daemon mount/unshare operations. |
| T-0059 | Design the `nc -N` port-forwarding correction, assess flag portability, and define Docker-free regression coverage. | User request 2026-08-24 / r1 | Done | T-0058 | Decide / Medium | None | Stop; implementation-ready design complete. | `.meta/tasks/0059-nc-half-close-design.md` | Designed a narrow `nc -N` connector change, a pure argv contract test, a real loopback/subprocess fake-Docker test, retention of the in-memory relay test and Docker smoke, and architecture updates. Confirmed `-N` is OpenBSD-specific: BusyBox and traditional netcat omit it, while Nmap Ncat half-closes by default without that short flag. The accepted short-term design therefore records a real compatibility limitation for preinstalled `nc` and the existing yum/dnf `nmap-ncat` fallback. |
| T-0060 | Implement reliable port-forward half-close behavior through a baked variant-aware `dcc-connect` wrapper, compatible connector provisioning, Docker-free regression coverage, and aligned architecture documentation. | User request and wrapper-scope follow-up 2026-08-24 / r2 | Done | T-0059 | Initiative / Medium | None | Stop; outcome complete. | `.meta/tasks/0060-port-forward-connector-brief.md`; `.meta/tasks/0059-nc-half-close-design.md`; `.meta/quality/0060-port-forward-connector-quality.md` | Implemented a baked `dcc-connect` boundary selecting OpenBSD netcat with `-N`, identified Nmap Ncat without it, and generic `nc` only when standalone `-N` is advertised. Generated builds check compatibility before and after apt/apk/yum/dnf fallbacks and no longer accept arbitrary `nc`. The host relay now drops its child stdin pipe on request EOF while draining stdout. Eleven forwarding and 35 build-context tests cover selection, unsupported variants, exact argv, executable assets, ordering, and a real subprocess response-after-EOF path. Format, check, clippy, 510 unit plus all runnable integration tests, build, diff, architecture, and threat-model review passed. CI then ran `forwarded_port_reaches_container_loopback_service` successfully twice and passed all 32 Docker smokes in both runs. |
| T-0061 | Diagnose the autotag CI failure evidenced by `logs_88712697210.zip`. | User request 2026-08-24 / r1 | Done | None | Discover / Medium | None | Stop; diagnosis complete and no product-code or workflow change was authorized. | None | The version bump pushed commit `07749f2` and autotag successfully created `v0.1.1`; the run failed afterward in release-gating CI. The sole failing check was `forward::tests::connector_prefers_openbsd_then_nmap_then_compatible_generic_nc` at checkout `0de0c24`, where spawning a just-written temporary `dcc-connect` script returned Linux `ETXTBSY` (`ExecutableFileBusy`). The same test and commit had passed minutes earlier, while format, Clippy, build, and both 32-test Docker-smoke runs passed. The fixture writes executable scripts while the parallel Rust suite spawns other processes, allowing a concurrent fork to transiently inherit a writable descriptor before `CLOEXEC`; this is a known `Command::spawn` race, not a connector behavior failure. Separately, release-gating CI checked stale commit `0de0c24` because its nested reusable CI checkout does not receive the tag ref, although the bump changed only version metadata. |
| T-0062 | Eliminate the fork/exec race in temporary executable test fixtures and make release-gating reusable CI check the requested tag ref. | User request 2026-08-24 / r1 | Done | T-0061 | Quick change / Medium | None | Stop; outcome complete. | `.meta/threat-models/0062-ci-ref-and-fixture.md` | Executable fixtures are now written entirely by a short-lived child process, so the parallel Rust test process never owns a writable descriptor that another fork can inherit; a bounded process-churn regression protects the boundary. Reusable CI accepts an optional `ref`, all five jobs check out that input or the triggering SHA, and release forwards its requested tag into CI. The 12 forwarding tests passed, the churn regression passed 50 consecutive runs, `actionlint 1.7.12` accepted every workflow, and format, check, Clippy, the full test suite (511 unit plus all runnable integration tests), build, and diff checks passed. |
| T-0063 | Remove duplicate CI from the autotag-to-release path while preserving one exact-commit CI gate for automatic bumps and an independent CI gate for direct tag pushes. | User request 2026-08-24 / r1 | Done | T-0062 | Quick change / Medium | None | Stop; outcome complete. | `.meta/threat-models/0063-release-ci-reuse.md` | The bump job now exports its newly committed SHA and passes it to autotag; automatic-push autotag uses the triggering SHA. Autotag runs reusable CI and creates the version tag from that same immutable commit, then calls release with `ci_already_passed: true`. Release skips only that redundant CI run; direct tag pushes still run CI, and release builds require either trusted reuse or an explicit successful CI result. Maintainer documentation and the CI threat model reflect the flow. Static ref/gate contract checks, `actionlint 1.7.12` across every workflow, format, check, Clippy, 511 unit plus all runnable integration tests, build, and diff checks passed. |
| T-0064 | Remove the test-only unused import and make Clippy lint every Cargo target locally and in CI. | User request 2026-08-24 / r1 | Done | None | Quick change / Low | None | Stop; outcome complete. | None | Removed the unused `AsyncWriteExt` test import, changed CI and current contributor guidance to `cargo clippy --all-targets -- -D warnings`, and aligned the canonical command catalog. Format, all-target Clippy, the full test suite (511 unit tests plus all runnable integration tests), `actionlint 1.7.12`, static workflow-command checks, and diff checks passed. |
| T-0065 | Remove GitHub Actions Node.js 20 deprecation warnings from every workflow action. | User request 2026-08-24 / r1 | Done | None | Quick change / Low | None | Stop; outcome complete. | None | Upgraded `actions/upload-artifact` from v5 to v6 and `actions/download-artifact` from v5 to v7. Upstream manifests confirm every external action now uses Node 24 or is composite. `actionlint 1.7.12`, the runtime audit, format, all-target Clippy, and the full test suite passed. One unrelated forwarding bind-release test failed once, then passed 20 focused runs and the complete rerun. |
| T-0066 | Bump the project patch version from 0.1.2 to 0.1.3 without pushing or triggering release automation. | User request 2026-08-24 / r1 | Done | None | Quick change / Low | None | Stop; outcome complete. | None | Updated `Cargo.toml` and the root `dcc` package entry in `Cargo.lock` to 0.1.3. Cargo metadata, format, check, all-target Clippy, the full test suite (511 unit tests plus all runnable integration tests), and build passed. No push, tag, or release workflow was triggered. |
| T-0067 | Fix the skipped `Create Release` job while preserving exact-commit CI reuse for automatic releases and CI gating for direct tag pushes. | User request 2026-08-25 / r1 | Done | None | Quick change / High | None | Stop; outcome complete. | `.meta/quality/0067-release-publication-gate-quality.md`; `.meta/threat-models/0063-release-ci-reuse.md`; `.meta/incidents/0067-skipped-release-publication.md` | Added an explicit `!cancelled()` plus successful aggregated-build condition to `Create Release`, so intentional reusable-CI skips no longer suppress publication and failed/cancelled builds still cannot publish. Added a CI-enforced release-workflow contract script covering CI reuse, direct-tag gating, build gating, and publication. Counterfactual failure, the corrected contract, Bash syntax, ShellCheck, `actionlint 1.7.12`, format, all-target Clippy, the full test suite, diff, threat-model, and consistency reviews passed. No push or release was triggered; the next authorized release remains the live scheduler confirmation. |
| T-0068 | Bump the project patch version from 0.1.3 to 0.1.4 so the repaired automatic-release path can be exercised after an owner push. | User request 2026-08-25 / r1 | Done | T-0067 | Quick change / Low | None | Stop; outcome complete. | None | Updated `Cargo.toml` and the root `dcc` package entry in `Cargo.lock` to 0.1.4. Cargo metadata, the release-workflow contract, `actionlint 1.7.12`, format, check, all-target Clippy, the full test suite (511 unit tests plus every runnable integration test), build, and diff review passed. No push, tag, or release workflow was triggered. |
| T-0069 | Build devcontainer specs whose OCI Feature archives contain a safe archive-root directory entry, including `ghcr.io/dc-powertools/features/sudo`, without weakening archive traversal or entry-type protections. | User request 2026-08-25 / r1 | Done | None | Quick change / Medium | None | Stop; outcome complete. | None | OCI extraction now ignores normalized empty paths only for directory entries such as `./`; empty non-directory entries, traversal, absolute paths, links, and special entries remain rejected. The pre-fix regression reproduced the reported error, direct extraction and local authenticated-registry tests cover the root marker, and a temporary production-client check successfully downloaded and extracted `ghcr.io/dc-powertools/features/sudo:latest`. Format, check, all-target Clippy, 513 unit tests plus all runnable integration tests, build, and diff review passed. |
| T-0070 | Coordinate the full design and implementation of supported custom-CA trust for private OCI Feature registries plus a TLS package-to-image Docker smoke, without weakening default HTTPS verification. | User request 2026-08-25 / r1 | Pending | T-0071, T-0072, T-0073 | Initiative / High | None | Complete the dependency-ordered child tasks, then reconcile their acceptance evidence and close the initiative. | `.meta/tasks/0070-custom-ca-registry-initiative.md` | Pending. |
| T-0071 | Decide the production custom-CA configuration and security contract, including registry binding, precedence, PEM/path validation, redirect and token-realm behavior, diagnostics, and the deterministic TLS OCI fixture design. | Parent T-0070; user request 2026-08-25 / r1 | Done | None | Decide / High | None | Stop; accepted design and handoff are complete. | `.meta/tasks/0070-custom-ca-registry-initiative.md`; `.meta/decisions/0007-registry-scoped-custom-ca.md`; `.meta/threat-models/0070-custom-ca-oci.md`; `.meta/quality/0071-custom-ca-design-quality.md` | Accepted exact-authority custom-CA contract, updated OCI threat model, dependency and fixture design, and exhaustive production/TLS/Docker test matrices after independent architecture and security review; artifact consistency and diff checks passed. |
| T-0072 | Implement the accepted opt-in custom-CA capability in the production OCI Feature client with public defaults preserved, no insecure fallback, deterministic success/failure tests, and user/architecture documentation. | Parent T-0070 / r1 | Ready | T-0071 | Initiative / High | None | Implement decision 0007 and the T-0072 matrix, including counterfactual TLS tests, documentation, and full Rust verification gates. | `.meta/tasks/0070-custom-ca-registry-initiative.md` | Pending. |
| T-0073 | Implement and CI-enable an ignored end-to-end smoke that packages a minimal Feature containing `./`, exposes it through ephemeral trusted TLS OCI infrastructure, runs the compiled `dcc build`, verifies the image marker, guarantees cleanup, and supplies closure evidence for T-0070. | Parent T-0070 / r1 | Pending | T-0072 | Initiative / High | None | After T-0072 is Done, build the fixture and smoke, add explicit serial Docker-CI execution, and pass workflow/full verification plus security review before parent reconciliation. | `.meta/tasks/0070-custom-ca-registry-initiative.md` | Pending. |
| T-0074 | Bump the project patch version from 0.1.4 to 0.1.5 without pushing or triggering release automation. | User request 2026-08-25 / r1 | Done | None | Quick change / Low | None | Stop; outcome complete. | None | Updated `Cargo.toml` and the root `dcc` package entry in `Cargo.lock` to 0.1.5. Cargo metadata, the release-workflow contract, `actionlint` 1.7.12, format, check, all-target Clippy, the full test suite (516 unit tests plus every runnable integration test), build, and diff review passed. One previously observed forwarding bind-release test failed once, then passed 20 focused repetitions and the clean full rerun. No push, tag, or release workflow was triggered. |
| T-0076 | Diagnose why dcc v0.1 rejects the latest dc-powertools Rust Feature when its `customizations.dcc.state` contains `${containerWorkspaceFolder}/target`. | User request 2026-08-25 / r1 | Done | None | Discover / Medium | None | Stop; diagnosis complete and no product-code change was authorized. | None | Reproduced the exact failure with dcc 0.1.5 and a local Feature fixture before Docker invocation. Project state runs `apply_state_path_substitution` before validation, resolving `${containerWorkspaceFolder}` to `/workspace`; Feature state instead goes directly from raw metadata to `validate_state_entries_allowing_deferred_container_env`, which permits deferred `${containerEnv:...}` only and rejects the workspace token as unresolved. The Rust Feature's 0.1.0 migration introduced this state block on 2026-08-24, exposing an omission present since dcc first added Feature state support. The same defect affects any Feature state using fixed container variables, including the current Node Feature. The appropriate dcc correction is to apply container-side state-path substitution consistently before Feature validation (and cover raw Feature and imported metadata-label paths); a Feature-side literal `/workspace/target` is a narrow workaround. Focused project-state substitution passed, and no product code was changed. |
| T-0077 | Make Feature-contributed `customizations.dcc.state` apply the same supported container-side path substitution as project state, so `${containerWorkspaceFolder}` and other fixed container variables are resolved before validation. | User request 2026-08-25 / r1 | Done | None | Quick change / Medium | None | Stop; outcome complete. | None | Centralized container-only state-entry substitution and applied it before validation for raw Feature metadata and nested or legacy imported `devcontainer.metadata` state. Fixed variables now normalize consistently with project state, deferred `${containerEnv:...}` remains image-aware, and host-local, reserved, relative, and overlapping paths retain their safety checks. Four counterfactual Feature tests failed on the old behavior; the corrected 61-test Feature module and full suite passed with 519 unit tests plus all runnable integration suites. Format, check, all-target Clippy, build, diff review, user/architecture documentation, and an independent zero-finding review passed. Live Docker/GHCR execution was not required for this pre-Docker metadata boundary. |
| T-0078 | Bump the project patch version from 0.1.5 to 0.1.6 without pushing or triggering release automation. | User request 2026-08-25 / r1 | Done | None | Quick change / Low | None | Stop; outcome complete. | None | Updated `Cargo.toml` and the root `dcc` package entry in `Cargo.lock` to 0.1.6. Cargo metadata, the release-workflow contract, `actionlint` 1.7.12, format, check, all-target Clippy, the full test suite (519 unit tests plus every runnable integration suite), build, and diff review passed. Installed `actionlint` 1.7.12 from its checksum-verified official arm64 release after the tool was absent from PATH. No push, tag, release, or workflow execution was triggered. |
| T-0075 | Harden `forward::tests::later_bind_collision_releases_every_listener_without_spawning_tasks` so it deterministically proves every earlier listener is released after a later bind collision, without relying on timing-sensitive port reuse. | User request 2026-08-25 / r1 | Done | None | Quick change / Medium | None | Stop; outcome complete. | None | Extracted deterministic listener acquisition and changed the regression to observe live kernel-assigned addresses, force a retained later collision, and prove every earlier listener is released by rebinding it. A deliberate listener-leak counterfactual failed immediately with `EADDRINUSE`; the corrected test passed 200 combined stress runs, all 12 forwarding tests, format, check, all-target Clippy, the full suite (519 unit and 67 runnable integration tests), build, diff check, and independent review. |
| T-0079 | Automatically place a `.gitignore` inside each dcc-managed `.dcc/` directory so generated profiles, caches, and state do not appear as untracked repository content. | User request 2026-08-25 / r1 | Ready | None | Quick change / Low | None | Identify every `.dcc/` creation path, implement non-destructive `.dcc/.gitignore` creation, and verify new and pre-existing directory behavior with focused tests and the standard Rust quality gates. | None | Pending. |

## Operating Contract

- Lifecycle, ownership, selection, detail, and archive rules:
  `.meta/meta/knowledge-management.md#task-lifecycle-and-selection`
- Intake and task-close behavior: `.meta/meta/root-loop.md`
- Interruption and message-targeting behavior: `.meta/meta/resumption-protocol.md`
- This file owns only project task facts. Do not add another work or priority list.
