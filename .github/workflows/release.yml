name: Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  release:
    uses: ./.github/workflows/_build-release.yml
    permissions:
      contents: write
    with:
      ref: ${{ github.ref_name }}
