# Task Catalog

This is the canonical discovery and lifecycle record for every accepted task. Physical
row order has no scheduling meaning. The Root Orchestrator is the sole writer.

- Format: 1
- Next task ID: T-0033
- Primary task: None
- Scheduling: Running
- Global pause source or reason: None

## Tasks

| ID | Outcome | Authority / Rev | Status | Depends On | Route / Risk | Approval Or Blocker | Next Safe Action | Details | Result |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| T-0001 | Load agent files and initialize the development framework state. | User request 2026-07-14 / r1 | Done | None | Discover / Low | None | Stop; outcome complete. | None | Created the project cursor, task catalog, project brief, implementation context, source map, glossary, and standards. Verified `cargo fmt --check`, `cargo check`, `cargo clippy -- -D warnings`, `cargo test`, `cargo build`, and `cargo run -- --help`. |
| T-0002 | Evaluate `AGENTS.bak.md` and unexpected `readme/` files, merge durable guidance into framework state, and remove leftovers. | User request 2026-07-14 / r1 | Done | None | Quick change / Low | None | Stop; outcome complete. | None | Migrated the backup's project-doc load rules, `.gitignore` boundary, and Codex question-handling guidance into project context, standards, and source map. Removed `AGENTS.bak.md`. Confirmed there are no untracked files. |
| T-0003 | Migrate `readme/DEVELOPMENT.md`, `readme/STYLE.md`, and `readme/ARCHITECTURE.md` to strict framework ownership. | User request 2026-07-14 / r1 | Done | None | Quick change / Low | None | Stop; outcome complete. | None | Moved the legacy docs to `readme/project/development.md`, `readme/project/rust-style.md`, and `readme/project/architecture.md`; updated cursor, context, standards, source map, glossary, and internal references so no unmanaged legacy doc paths remain. |
| T-0004 | Coordinate the full `dcc` rewrite for devcontainer schema-compatible config, state persistence, durable container lifecycle commands, and shell-oriented attach behavior. | User request 2026-07-14 + attach amendment + full-framework execution request / r3 | Done | Child tasks T-0005 through T-0010 | Initiative / High | None | Stop; outcome complete. | `readme/tasks/0004-dcc-rewrite-brief.md`; `readme/tasks/0004-dcc-rewrite-notes.md`; `readme/quality/0004-dcc-rewrite-quality.md`; `readme/threat-models/0004-dcc-runtime.md` | Completed schema-compatible `customizations.dcc`, state persistence, Feature metadata compatibility, build preparation, durable runtime lifecycle commands, final compatibility fields, unsafe runtime gating, docs, review, and follow-up official devcontainer CLI config validation. Live Docker smoke testing remains a recorded residual risk. |
| T-0005 | Implement devcontainer schema-compatible config parsing and merge behavior under `customizations.dcc`, including legacy `extends`/`scripts` warnings. | Parent T-0004 / r1 | Done | None | Initiative / High | None | Stop; outcome complete. | `readme/tasks/0005-config-schema-brief.md` | Added `customizations.dcc.extends`, `commands`, and parser-level `state` support; normalized legacy top-level `extends`/`scripts` with deprecation warnings; preserved existing command resolution semantics; verified fmt, clippy, tests, and build; completed read-only specialist review with no blocking findings. |
| T-0006 | Implement validated `customizations.dcc.state` path model and profile-local state cache mount planning. | Parent T-0004 / r1 | Done | T-0005 | Initiative / High | None | Stop; outcome complete. | `readme/tasks/0006-state-cache-brief.md` | Added state path substitution/validation, duplicate/conflict and overlap rejection, deferred runtime `${containerEnv:VAR}` resolution, profile-local state mount planning, host preparation for directory/file state, runtime/debug mount integration, docs, and tests. Verified fmt, clippy, tests, and build. |
| T-0007 | Update Feature metadata handling for state, commands, unsupported properties, unsafe runtime settings, and lifecycle hook collection order. | Parent T-0004 / r1 | Done | T-0005, T-0006 | Initiative / High | None | Stop; outcome complete. | `readme/tasks/0007-feature-metadata-brief.md` | Added Feature `customizations.dcc.commands` and `state`, legacy `scripts` warnings, Feature state metadata/runtime merge, unsupported `init`/`entrypoint` warnings, Feature user-property errors, unsafe Feature runtime gating via `--allow-unsafe-runtime`, docs, and tests. Verified fmt, clippy, tests, and build. |
| T-0008 | Implement build preparation: official `build` source support, generated controller/hook assets, default prep hooks, and `build --refresh-only`. | Parent T-0004 / r1 | Done | T-0005, T-0006, T-0007 | Initiative / High | None | Stop; outcome complete. | `readme/tasks/0008-build-prep-brief.md` | Added official `build` source support, image/build conflict validation, generated controller/wrapper/hook assets, build-preparation hook execution, `build --refresh-only`, docs, and tests. Verified fmt, clippy, tests, and build. |
| T-0009 | Implement durable runtime lifecycle commands: `start`, `stop`, `run`, `exec`, `attach`, one-shot bookkeeping, and `--keep` promotion. | Parent T-0004 / r1 | Done | T-0008 | Initiative / High | None | Stop; outcome complete. | `readme/tasks/0009-runtime-lifecycle-brief.md` | Added `start`, `attach`, `--keep`/`-k`, durable promotion, existing-container reuse, one-shot active-command bookkeeping, scoped startup/attach hooks, docs, and tests. Verified fmt, clippy, tests, and build. |
| T-0010 | Complete port attributes, safe `runArgs`, documentation, fixtures, official config validation, strict review, and final parent closure. | Parent T-0004 / r1 | Done | T-0005, T-0006, T-0007, T-0008, T-0009 | Initiative / High | None | Stop; outcome complete. | `readme/tasks/0010-final-validation-brief.md` | Added final compatibility parsing, conservative `runArgs`, devcontainer unsafe runtime gating, sensitive mount protections, `workspaceFolder` workdir support, docs, strict review fixes, and parent closure. Verified fmt, clippy, tests, build, and follow-up official devcontainer CLI config validation. |
| T-0011 | Install missing validation tooling and run official Dev Container CLI config validation for the completed rewrite. | User resume and sudo/tooling approval 2026-07-15 / r1 | Done | T-0004 | Quick change / Low | None | Stop; outcome complete. | `readme/quality/0004-dcc-rewrite-quality.md`; `readme/tasks/0004-dcc-rewrite-notes.md` | Installed Node.js v20.19.2, npm 9.2.0, `@devcontainers/cli 0.87.0`, and Docker 26.1.5; started a constrained Docker daemon for CLI inspection; `sudo devcontainer read-configuration --workspace-folder /workspace --include-merged-configuration --log-level trace` passed and produced merged configuration output. Ignored live Docker smoke was attempted but blocked by harness mount/namespace restrictions. |
| T-0012 | Enable Docker-dependent smoke tests in GitHub Actions while keeping local development-container tests Docker-free by default. | User request 2026-07-20 / r1 | Done | T-0011 | Quick change / Low | None | Stop; outcome complete. | None | Added a `docker-test` CI job on GitHub-hosted Ubuntu runners that verifies Docker availability and explicitly runs the ignored Docker smoke tests. Updated test comments and standards to record that local `cargo test` remains Docker-free while CI owns live Docker build/run coverage. Verified workflow syntax with `actionlint`, listed the ignored Docker tests without running them locally, and passed fmt, clippy, tests, and build. |
| T-0013 | Add Docker-free dry-run validation and JSON output for CLI acceptance tests. | User request 2026-07-20 / r1 | Done | T-0012 | Quick change / Medium | None | Stop; outcome complete. | None | Added global `--dry-run` and `--format text\|json` flags, stable dry-run reports, Docker-boundary exits for build/runtime/stop commands, JSON output for `dcc id`, docs, and CLI tests that use dry-run success instead of fragile Docker error parsing. Kept ignored Docker smokes for real build/exec/run coverage. Verified fmt, tests, clippy, build, help output, dry-run JSON output, ignored-test listing, and workflow lint with `actionlint 1.7.12`. |
| T-0014 | Add expanded Docker behavior smoke tests for the completed rewrite. | User request 2026-07-20 / r1 | Done | T-0012, T-0013 | Quick change / Medium | None | Stop; outcome complete. | None | Added ignored `tests/docker_smoke.rs` coverage for project named commands, lifecycle hook phase separation, state directory/file persistence across one-shot containers and refresh-only builds, durable vs one-shot container reuse, `workspaceFolder` workdir behavior, runtime environment substitution, and local Feature command/state metadata. Updated CI to run the suite serially on GitHub Docker runners. Verified fmt, tests, clippy, build, actionlint, and ignored-test listings locally; live Docker execution remains delegated to GitHub runners. |
| T-0015 | Update user-facing documentation to match the completed `dcc` rewrite design and implementation. | User request 2026-07-20 / r1 | Done | T-0004, T-0013, T-0014 | Quick change / Low | None | Stop; outcome complete. | None | Reworked the README around the current mental model, common workflows, `customizations.dcc` config, explicit build requirements, named `dcc run` commands versus direct `dcc exec`, one-shot/durable reuse, dry-run/JSON output, and documented differences from normal devcontainers including ignored compatibility fields, port forwarding, unsafe runtime gating, and unsupported top-level `remoteUser`. Verified `git diff --check`, stale README searches, and `cargo test`. |
| T-0016 | Remove `initializeCommand` execution and keep it parse-and-warn only. | User correction 2026-07-20 / r1 | Done | T-0004 | Quick change / Medium | None | Stop; outcome complete. | None | Removed host execution of `initializeCommand`, deleted the host lifecycle executor, excluded `initializeCommand` from runtime `${containerEnv:...}` probing and debug lifecycle output, preserved parser compatibility with an unsupported-field warning, and updated README/architecture docs. Added CLI coverage for the warning and Docker smoke coverage that would fail if the hook mutates the host workspace. Verified fmt, tests, clippy, diff check, and stale-reference search. |
| T-0017 | Reorganize README lifecycle-hook guidance into a dedicated trigger table. | User request 2026-07-20 / r1 | Done | T-0016 | Quick change / Low | None | Stop; outcome complete. | None | Moved lifecycle-hook behavior into its own README section, added a hook-by-hook table showing which `dcc` commands trigger or skip each hook, called out `dcc id`/`dcc stop`, and reduced duplicate hook behavior in the configuration table. Verified `git diff --check` and targeted lifecycle README searches. |
| T-0018 | Move shared README command flag guidance into a dedicated global flags subsection. | User request 2026-07-20 / r1 | Done | T-0017 | Quick change / Low | None | Stop; outcome complete. | None | Moved `--strict`, `--dry-run`, `--format json`, and launch debugging guidance into `Commands` > `Global flags`, removed the old dry-run and debugging subsections, and left command-specific sections focused on subcommand behavior. Verified `git diff --check` and targeted README heading/flag searches. |
| T-0019 | Make `--debug` a global CLI flag supported by all commands. | User request 2026-07-20 / r1 | Done | T-0018 | Quick change / Medium | None | Stop; outcome complete. | None | Moved `--debug` from runtime subcommands to the global CLI options, threaded it through existing runtime launch debugging, added build/stop/id debug output for resolved profile/config/container/image details, updated README and architecture docs, and added CLI tests for global placement plus non-runtime debug output. Verified fmt, tests, clippy, help output, grep checks, and diff check. |
| T-0020 | Design and implement a `feature` subcommand that accepts `--add`/`-a` and `--remove`/`-r` to add and remove features from the profile. | User request 2026-07-21 / r1 | Done | None | Quick change / Medium | None | Stop; outcome complete. | None | Added `dcc feature` with repeatable `--add`/`-a` and `--remove`/`-r`, Docker-free dry-run/debug/JSON output support, JSONC profile parsing with formatted JSON writes, docs, and integration coverage. Verified fmt, check, tests, clippy, build, diff check, help output, and dry-run JSON output. |
| T-0021 | Guard `customizations.dcc.state` against critical container paths, including the currently unguarded `/cache` self-nesting bug and `dcc`'s own `/usr/local/share/dcc` assets. | User request 2026-07-21 / r1 | Done | None | Quick change / Medium | None | Stop; outcome complete. | `readme/tasks/0021-state-path-guards-brief.md` | Added two-tier reserved-path guards to `normalize_state_path` (subtree + exact), applied at config load and post-`${containerEnv:VAR}` resolution. Tier 1 blocks `/proc`, `/sys`, `/dev`, `/tmp`, `/run`, `/var/run`, `/var/lock`, `/boot`, `/bin`, `/sbin`, `/lib*`, `/usr/bin`, `/usr/sbin`, `/usr/lib*`, `/etc`, `/workspace/.dcc`, `/cache`, `/usr/local/share/dcc` (subtree). Tier 2 blocks `/usr`, `/var`, `/home`, `/root`, `/opt`, `/workspace`, `/srv`, `/mnt`, `/media` (exact only; children accepted). Errors name the path, matched reserved path, and an alternative. Updated README and architecture reserved-path docs. Verified fmt, clippy, tests (439 unit), and build. |
| T-0022 | Seed declared state from the image so Feature- and Dockerfile-installed content is not silently masked by empty bind mounts, and survives a wiped `.dcc`. | User request 2026-07-21 / r1 | Done | T-0021 | Initiative / High | None | Stop; outcome complete. | `readme/tasks/0022-state-seeding-brief.md`; `readme/decisions/0001-state-seeding-from-image.md` | Added `src/seed.rs`: `dcc build` hydrates declared state from the image via a one-shot container (state mounts off, host state root at `/dcc-seed`) copying with `tar` inside the container before build-prep hooks; a `dcc.seed` image label carries the resolved manifest; a host-side `.dcc/<profile>.seed.json` ledger records `seed_digest`/`build_id`; re-seed policy (match→skip, differ→preserve+warn, `--reseed-state`→overwrite); runtime guard on start/run/exec/attach warns on `build_id` mismatch and hydrates unrecorded entries; `--dry-run` reports planned seeding. Added `--reseed-state` CLI flag, `docker::run_to_completion`, and 8 ignored Docker smoke tests. Updated README and architecture. Verified fmt, clippy, tests, build, help output, and dry-run JSON. Live Docker execution delegated to CI. |
| T-0023 | Contain `dcc`'s host-side runtime bookkeeping so container-side code cannot reach it through the `/cache` mount. | Accepted in-scope finding 2026-07-21 / r1 | Superseded by T-0024 | None | Quick change / High | None | Stop; superseded by T-0024 which removes the host-side bookkeeping entirely. | `readme/tasks/0023-cache-runtime-exposure-brief.md` | Superseded by T-0024. T-0024 relocates lifecycle ownership into an in-container PID 1 supervisor, eliminating the host-side bookkeeping this task was opened to contain. |
| T-0024 | Move container lifecycle ownership into an in-container PID 1 supervisor; remove host-side file-based runtime bookkeeping and the `mkdir` lock; add `dcc stop` graceful / `--now` / `--kill` variants. | Architecture decision 2026-07-21 / r1 | Done | None | Initiative / High | None | Stop; outcome complete. | `readme/tasks/0024-supervisor-lifecycle-ownership-brief.md`; `readme/tasks/0024-r1-supervisor-design.md` | Implemented in-container PID 1 lifecycle supervisor (`src/supervisor.rs`): POSIX sh scripts bind-mounted read-only from `.dcc/<profile>.rt/`; state in tmpfs `/run/dcc`; one-shot self-teardown on drain; `dcc stop` graceful/--now/--kill. Removed `src/runtime.rs`; reworked exec.rs, stop.rs, build.rs; removed dead dcc-controller/dcc-command assets; added docker::kill_container. Updated threat model, README, architecture. T-0023 superseded. Verified fmt, clippy (0 prod warnings), 506 tests, build. |
| T-0024-R1 | Research and design the supervisor: implementation language, CLI↔entrypoint↔supervisor message protocol, active-set and mode state machine, and `stop` graceful/`--now`/`--kill` semantics. Produce a design document for review. | Parent T-0024 / r1 | Done | None | Design / High | None | Stop; outcome complete. | `readme/tasks/0024-r1-supervisor-design.md` | Design document produced. |
| T-0024-S1 | Implement the PID 1 supervisor and command entrypoint: active-set state machine, mode handling, graceful-stop gating, and `--now` shutdown-hook execution; generate as in-container assets. | Parent T-0024 / r1 | Done | T-0024-R1 | Initiative / High | None | Stop; outcome complete. | None | Implemented `src/supervisor.rs` with 8 unit tests. |
| T-0024-H1 | Rework the host CLI (`src/exec.rs`, `src/stop.rs`, `src/docker.rs`) to drive the supervisor through the entrypoint; remove `src/runtime.rs` and all host-side bookkeeping file I/O; add `dcc stop` graceful / `--now` / `--kill` variants. | Parent T-0024 / r1 | Done | T-0024-S1 | Initiative / High | None | Stop; outcome complete. | None | Reworked exec/stop; deleted runtime.rs; added stop variants + kill_container; 3 CLI tests. |
| T-0024-B1 | Update the build-preparation container (`src/build.rs`) and generated controller/command assets (`src/features/mod.rs`) to use the supervisor as PID 1. | Parent T-0024 / r1 | Done | T-0024-S1 | Quick change / Medium | None | Stop; outcome complete. | None | Updated build_prep_container_args; removed dead dcc-controller/dcc-command assets. |
| T-0024-T1 | Add tests: supervisor state-machine unit tests, entrypoint mediation tests, `stop` variant behavior, and ignored Docker smoke tests asserting no host-side bookkeeping exists and teardown/reuse/stop behave correctly. | Parent T-0024 / r1 | Done | T-0024-H1, T-0024-B1 | Quick change / Medium | None | Stop; outcome complete. | None | 8 supervisor unit tests; 3 CLI tests; 5 ignored Docker smoke tests. |
| T-0024-D1 | Update `readme/threat-models/0004-dcc-runtime.md`, README, and `readme/project/architecture.md` to reflect supervisor ownership and the removed trust boundary; close T-0023 as superseded. | Parent T-0024 / r1 | Done | T-0024-H1 | Quick change / Low | None | Stop; outcome complete. | None | Updated architecture.md, README, threat model; T-0023 marked superseded. |
| T-0025 | Move startup sequencing and runtime lifecycle hooks into the supervisor: `--mode`/`--expect-command` entrypoint args, supervisor-run `postStartCommand`, a `dcc-ctl wait-ready` readiness probe the command harness blocks on after registering, and deletion of the time-based startup grace. | User direction 2026-08-13 / r2 | Done | T-0024 | Initiative / High | None | Stop; outcome complete. | `readme/tasks/0025-supervisor-startup-handshake-brief.md`; `readme/tasks/0025-r1-startup-handshake-design.md` | Implemented per the r3 design: supervisor accepts `--mode`/`--expect-command`/`--start-hooks` entrypoint args (retired `DCC_MODE` env); runs `postStartCommand` hooks (feature-first, then devcontainer) inside PID 1 from host-generated pre-substituted scripts in `.dcc/<profile>.rt/start-hooks/`; writes a single `/run/dcc/bootstrap-status` file (`0` or `<exit-code> <hook-name>`); `dcc-ctl wait-ready` uses a per-waiter FIFO handshake (mkfifo before status check, `<>` open to avoid wedging on orphaned FIFOs) — no polling, no package dependency; `dcc-exec` registers then waits for readiness before running the command; deleted `STARTUP_GRACE_SECS`/`PRIMED`/grace branch, replaced with a supervisor-local `arrived` variable + 10 s one-shot post-bootstrap orphan reaper; `postAttachCommand` stays host-side with a cold-start `wait-ready`; host maps exit 252 to a clear hook-failure error. `${containerEnv:…}` substitution stays host-side (no config-compatibility change). Updated `src/supervisor.rs` (full rewrite of all three scripts + hook-script generation + `sh_quote` + `RtDir::write_start_hooks`/`has_start_hooks`), `src/exec.rs` (entrypoint args, start-hook emission, `wait_ready`, removed `run_runtime_hooks(Startup)`), `src/build.rs` (`--mode durable` entrypoint arg). 478 unit tests (incl. 6 real-`sh` execution tests for bootstrap/failure/FIFO/fast-path/252), 33 ignored Docker smokes (9 new: cold-start ordering, slow hook, failing hook, start+stop variants, skip-lifecycle, drain, durable reuse). Verified fmt, clippy (0 warnings incl. `--all-targets`), test, build. Live Docker execution delegated to CI. Fast-path removal split out as T-0027 (not a prerequisite). |
| T-0026 | Implement the spec's `updateRemoteUserUID` (default `true`, Linux-only) so a non-root `containerUser` can write to bind-mounted workspace, cache, and state regardless of host uid; restore `seeded_directory_is_writable_by_container_user` to its original workspace-writing form. | User direction 2026-08-13 / r1 | Done | None | Initiative / High | None | Stop; outcome complete. | `readme/tasks/0026-update-remote-user-uid-brief.md`; `readme/tasks/0026-r1-uid-remap-design.md` | Added `updateRemoteUserUID` (recognized config field, default `true`, child-overrides-parent merge) and `src/uid.rs`: `plan_uid_remap` with every reference no-op (root, numeric, already-matching, uid collision, gid-collision-keeps-gid, non-Linux, disabled, host-ids-unavailable), `host_ids` via `id -u`/`id -g`, and `remap_dockerfile_block`/`remap_build_args` baking the `devcontainers/cli` `updateUID.Dockerfile` logic into dcc's generated build stage as a `RUN` after user creation with `--build-arg REMOTE_USER/NEW_UID/NEW_GID`. Wired into `build_dcc_stage` (`docker::build` now forwards build args), `--debug`, and `--dry-run` reports. Restored `seeded_directory_is_writable_by_container_user` to `/workspace/writable.txt` + `fx.read_file`; added ignored Docker smoke tests for non-root writes to `/workspace`/`/cache`/state, in-container uid matching host, and root fast-path unaffected. Updated README and architecture. Seeding interaction resolved: the remap is baked at image build, so T-0022 hydration already sees the remapped `/etc/passwd` and seeded content is owned by the remapped user. Verified fmt, clippy (0 warnings), 463 unit tests, build, dry-run JSON. Live Docker execution delegated to CI. |
| T-0027 | Remove the `image` fast path (`uses_fast_path`) so every dcc container is uniformly built from a dcc image stage, and drop the fast-path special cases in `build.rs`, `version.rs`, `run.rs`, `stop.rs`, `exec.rs`, and `uid.rs`. | User direction 2026-08-13 / r2 | Done | None | Initiative / Medium | None | Stop; outcome complete. | `readme/tasks/0027-remove-image-fast-path-brief.md`; `readme/decisions/0003-remove-image-fast-path.md` | Deleted `uses_fast_path` and its pull+tag branch in `build.rs`; every build now runs `build_base_image` + `build_dcc_stage`, so every image carries a `dcc.version` label. C1: added `pull: bool` to `DockerBuildOptions`, emit `--pull` in `build_args` when set, plumbed `opts.update`→`pull` through `build_base_image` (build-source path) and `build_dcc_stage` (dcc stage); `--update` help text documents the base-image re-pull. C2: `write_lockfile` skips the write when `lock_entries` is empty and no lockfile exists (an existing lockfile is still rewritten). C3: deleted `docker::pull`/`docker::tag` (fast path was their only caller). Dropped `current_uses_fast_path` param from both `version.rs` entry points; `version_warning` collapses three `None` arms into one (missing label now always warns). Removed fast-path plumbing in `exec.rs`, `run.rs`, `stop.rs` (incl. the local `current_uses_fast_path` helper), and the vacuous `fast_path_config_implies_no_remap` test in `uid.rs`. `--no-cache` now honored for all profiles (fast path used to discard it). Deleted 10 tests (7 fast-path + 2 version + 1 uid), added 2 `--pull` flag tests. Updated docker_smoke `fast_path_root_profile_unaffected_by_uid_remap`→`root_image_profile_builds_with_version_stamp_and_no_remap` (now asserts the version label). Updated README + architecture fast-path prose. 470 unit tests, 29 integration, 33 ignored Docker smokes; fmt, clippy (`--all-targets -D warnings`), check, test, build all clean. `rt` bind mount unchanged (T-0028 tracks the delivery-model question). |
| T-0028 | Reconsider the `rt` bind mount and supervisor delivery model now that removing the fast path lifts the constraint that forced it; decide between the read-only bind mount, baking assets into the image, or a hybrid, and record the outcome. | User direction 2026-08-13 / r1 | Done | T-0027 | Design / Medium | None | Stop; outcome complete. | `readme/tasks/0028-supervisor-delivery-model-brief.md`; `readme/decisions/0004-embed-supervisor-in-image.md` | Decided: **bake the supervisor scripts into the image, keep `postStartCommand` hook scripts on the `rt` bind mount**, and add a semver compatibility gate. Q1: version skew handled by interpreting the `dcc.version` label with semver rules (patch = compatible, major/minor or missing label = refuse) instead of the current warning; patch releases may not change the host↔supervisor protocol. Q2: root-user tampering with baked scripts declared an explicit non-goal; the read-only mount property was found not load-bearing for the threat model. Q3 (decisive): hooks cannot be baked because `apply_substitutions` resolves `${localEnv:VAR}` in `lifecycle` from the invoking process's env, so hook text is only fully resolvable at run time — the codebase already encodes this rule by denying `localEnv` to `containerEnv`, which *is* baked. Also found that the baked build-prep `.sh` assets are never executed (the host `docker exec`s a freshly substituted argv), so they are vestigial. Q4/Q5/Q6 excluded from the evaluation per owner direction. Corrected stale rationale in T-0024 D1 and T-0025 D0/D2; updated the threat model scope and added an accepted-risk row. Implementation split to T-0029 and T-0030. |
| T-0029 | Bake the supervisor scripts (`dcc-supervisor`, `dcc-ctl`, `dcc-exec`) into the image via the build context, retarget the entrypoint and exec paths off the `rt` mount, shrink `RtDir` to manage only `start-hooks/`, and handle the build-prep container's now-possibly-empty mount. | Decision 0004 (T-0028) / r1 | Ready | T-0028, T-0030 | Initiative / Medium | None | Read `readme/decisions/0004-embed-supervisor-in-image.md`, then write a brief. Land T-0030's compatibility gate first or together, since a baked supervisor makes an incompatible image a protocol hazard. | `readme/decisions/0004-embed-supervisor-in-image.md` | Pending. |
| T-0030 | Replace the `dcc.version` mismatch warning with a semver compatibility gate for runtime commands (patch-level drift proceeds; major/minor drift or a missing label refuses with the rebuild command), and record the "no host↔supervisor protocol change in a patch release" rule in project standards. | Decision 0004 (T-0028) / r1 | Ready | T-0028 | Quick change / Medium | None | Read `readme/decisions/0004-embed-supervisor-in-image.md` Q1, then rework `src/version.rs` (`version_warning` becomes a compatibility decision; `dcc build` stays exempt). | `readme/decisions/0004-embed-supervisor-in-image.md` | Pending. |
| T-0031 | Remove the vestigial build-prep hook `.sh` assets baked into the image at `/usr/local/share/dcc/hooks/build-prep/`; they are never executed because the host `docker exec`s a freshly substituted argv via `run_planned_hooks`/`run_in_container`. | Discovered in-scope finding during T-0028 / r1 | Ready | None | Quick change / Low | None | Confirm no execution path reads the baked scripts (only a reserved-path test in `config/resolve.rs` and asset-order tests in `features/mod.rs` reference them), then drop `build_prep_hook_assets` and, if `generated_assets` becomes empty, the `COPY .dcc-generated/` step and its `install_generated_assets` flag. | None | Pending. |
| T-0032 | Decide whether `postStartCommand` hook scripts should be baked into the image alongside the supervisor (T-0029) or remain host-generated on the `rt` bind mount as T-0028 provisionally concluded; the blocker is `${localEnv:VAR}`, which is only resolvable at run time from the invoking user's environment. | User direction during T-0028 closure / r1 | Ready | T-0028 | Design / Medium | None | Read `readme/tasks/0032-whether-to-bake-hooks-brief.md`, which documents the `${localEnv:VAR}` constraint and the execution-mechanism asymmetry in full; answer Q1-Q4 and record a decision. If it reverses T-0028's provisional hook conclusion, amend decision 0004 Q3 and update T-0029's scope. | `readme/tasks/0032-whether-to-bake-hooks-brief.md` | Pending. |

## Operating Contract

- Lifecycle, ownership, selection, detail, and archive rules:
  `readme/meta/knowledge-management.md#task-lifecycle-and-selection`
- Intake and task-close behavior: `readme/meta/root-loop.md`
- Interruption and message-targeting behavior: `readme/meta/resumption-protocol.md`
- This file owns only project task facts. Do not add another work or priority list.
